export const metadata = {
    title: "Joseph Thenara - Digital Namecard",
    description: "Scan to connect with Joseph Thenara.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}